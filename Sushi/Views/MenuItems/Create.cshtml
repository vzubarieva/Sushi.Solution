@{
  Layout = "_Layout";
}

@model Sushi.Models.MenuItem

<h4>Add a new menu item</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.MenuItemName)
  @Html.TextBoxFor(model => model.MenuItemName)

  @Html.LabelFor(model => model.Customer)
  @Html.DropDownList("CustomerId")


  <input type="submit" value="Add new menu item" class="btn btn-primary" />
}

@Html.ActionLink("Show all menu items", "Index", new { })
