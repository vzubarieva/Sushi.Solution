<nav class="main-nav">
  
  @Html.ActionLink("Home", "Index", "Home", null, new { @class = "btnHome" })

  @Html.ActionLink("Menu", "Menu", "MenuItems", null, new { @class = "btn" })

  @Html.ActionLink("Order Online", "Index", "MenuItems",null, new { @class = "btn" })
  
  @Html.ActionLink("Customer", "Index", "Customers", null, new { @class = "btn" })
  
  
  @Html.ActionLink("AboutUs", "AboutUs", "MenuItems", null, new { @class = "btn" })

  @if (User.Identity.IsAuthenticated)
    {
      @using (Html.BeginForm("LogOff", "Account"))
      {
        <button type="submit" class="btn">Log out</button>
      }
    }
    else
    {
      @Html.ActionLink("Register", "Register", "Account", new {}, new {@class ="btn"})
      @Html.ActionLink("Log in", "Login", "Account", new {}, new {@class ="btn"})
    }
</nav>
